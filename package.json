{
  "name": "demo-txiki-module",
  "version": "1.0.0",
  "description": "Demo module for txiki.js",
  "type": "module",
  "scripts": {
    "runtime-types": "bun ./scripts/runtime.ts fast-clone",
    "bundle": "bun ./scripts/bundle.ts",
    "dev:setup": "meson setup build",
    "dev:build": "cd build && meson compile",
    "dev:test": "cd build && meson test"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.32.31"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "commander": "^12.1.0",
    "dts-bundle-generator": "^9.5.1"
  }
}
